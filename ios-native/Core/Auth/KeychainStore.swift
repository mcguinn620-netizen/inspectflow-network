// KeychainStore (legacy) has been superseded by InspectFlowConnector's SessionStore,
// which persists the auth session in the Keychain under the service
// "com.autoinspectornetwork.ios" / account "session". Kept intentionally empty so
// older references compile; safe to delete in a future cleanup pass.

import Foundation
