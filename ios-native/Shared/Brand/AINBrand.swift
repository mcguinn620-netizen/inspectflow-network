import SwiftUI

/// Branding tokens for Auto Inspector Network.
enum AINBrand {
    static let displayName = "Auto Inspector Network"
    static let shortName = "AIN"
    static let tagline = "The inspector's command center"

    /// Deep Navy
    static let primary = Color(red: 0.06, green: 0.10, blue: 0.20)
    /// Electric Blue
    static let accent = Color(red: 0.18, green: 0.51, blue: 0.95)
    static let pass = Color(red: 0.13, green: 0.70, blue: 0.47)
    static let warn = Color(red: 0.96, green: 0.66, blue: 0.18)
    static let fail = Color(red: 0.95, green: 0.32, blue: 0.42)
}
