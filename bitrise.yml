---
format_version: '26'
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git
project_type: ios
workflows:
  build:
    summary: Compile the native SwiftUI app for the iOS Simulator.
    steps:
    - git-clone@8: {}
    - xcode-build-for-test@3:
        inputs:
        - project_path: "$BITRISE_PROJECT_PATH"
        - scheme: "$BITRISE_SCHEME"
        - destination: platform=iOS Simulator,name=Bitrise iOS default,OS=latest
        - automatic_code_signing: 'off'
    - deploy-to-bitrise-io@2:
        inputs:
        - deploy_path: "$BITRISE_DEPLOY_DIR"
  archive_and_export_app:
    summary: Archive VehicleInspectorsApp and export an IPA.
    steps:
    - git-clone@8: {}
    - certificate-and-profile-installer@1: {}
    - set-ios-product-bundle-identifier@1:
        inputs:
        - project_path: $BITRISE_PROJECT_PATH
        - new_bundle_identifier: KADM.AutoInspectorNetwork
    - xcode-archive@6:
        inputs:
        - project_path: "$BITRISE_PROJECT_PATH"
        - scheme: "$BITRISE_SCHEME"
        - distribution_method: "$BITRISE_DISTRIBUTION_METHOD"
        - automatic_code_signing: 'off'
        - platform: iOS
        - verbose_log: 'yes'
        - xcodebuild_options: PRODUCT_BUNDLE_IDENTIFIER="KADM.AutoInspectorNetwork.ios”
    - deploy-to-bitrise-io@2: {}
    triggers:
      pull_request:
      - target_branch: main
meta:
  bitrise.io:
    stack: osx-xcode-26.4.x
    machine_type_id: g2.mac.large
app:
  envs:
  - BITRISE_PROJECT_PATH: ios-native/VehicleInspectorsApp.xcworkspace
    opts:
      is_expand: true
  - BITRISE_SCHEME: VehicleInspectorsApp
    opts:
      is_expand: true
  - BITRISE_DISTRIBUTION_METHOD: development
    opts:
      is_expand: true